#!/bin/shset -ehost="$1"shiftport="$1"shiftcmd="$@"echo "Waiting for $host:$port to be available..."while ! nc -z "$host" "$port"; do  sleep 1doneecho "$host:$port is available. Starting app..."exec $cmd